import './InfoCard.css';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

export default function InfoCard({ icon, title, text, btnText }) {
  return <div className="info__card"></div>;
}
